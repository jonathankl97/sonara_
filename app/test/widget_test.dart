import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Sonara app smoke test', (WidgetTester tester) async {
    // Placeholder test - wird in Sprint 4 mit echten Tests ersetzt
    expect(true, isTrue);
  });
}